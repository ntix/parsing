import { DateParsableTypes } from './DateParsableTypes';
/**
 * Parse a date, throw if not a date
 *
 * @param value to be parsed
 * @returns a date or null
 */
export declare function parseDate(value: DateParsableTypes): Date;
